extends layout
block content
    script(src="/js/socket.js")
    script(src="/js/main.js")
    - for (var i = 1;i <= 3;i++)
        table.page(pageInd=i)
            tbody
                each category in Object.keys(questions[i])
                    tr.category
                        td.category-title= category
                        - var points = 0
                        each question in questions[i][category]
                            - points += i * 100
                            td
                                div.question(question=question)
                                    span= points
    .currQuestion.hidden
        img.currQuestion-image
        .currQuestion-audio
        .currQuestion-video
        .currQuestion-text.currQuestion-text-small
        .currQuestion-queue